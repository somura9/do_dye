<div class="w-full max-w-xs mx-auto">
  <p class="text-gray-900 text-2xl dark:text-white font-bold mb-3">作品を追加する</p>
  <div class="md:flex md:items-center mb-6">
    <%= render 'form', { work: @work } %>
  </div>
</div>