<section class="bg-white dark:bg-gray-900">
  <%= render 'shared/search', url: root_path %>
  <div class="py-8 px-4 mx-auto max-w-screen-xl lg:py-16 lg:px-6 ">
      <div class="grid gap-8 mb-6 lg:mb-16 md:grid-cols-3">
              <% if @works.present? %>
              <%= render @works %>
             <% else %>
              <p><%= t('.no_result') %></p>
            <% end %>
      </div>
  </div>
</section>